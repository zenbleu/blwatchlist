Redesign Overview Rewatch Picks to match the attached stacked-card reference

The previous redesign did NOT match the intended design. Please revise it substantially.

Use the attached GIF/reference image as the primary visual reference. I want the rewatch picks to behave and look like a large stacked poster/card carousel, NOT the current large rectangular information cards.

Core visual concept

The reference has a large vertical poster as the main card, with additional cards/posters visibly stacked behind it to create a layered/depth effect.

For the BL Watchlist Overview:

The poster should be the dominant visual element.
Cards should be portrait-oriented, following the natural poster aspect ratio.
Multiple entries should form a horizontal stack/carousel across the available desktop width.
Cards should visually overlap/stack rather than appearing as independent rectangular panels in a conventional 2-column grid.
The stack should have visible depth, similar to the reference.
Use the available desktop width much more effectively.
Do NOT use the previous redesign's large horizontal rectangular cards with a poster on the left and text on the right.
Card composition

Each rewatch-pick card should primarily be a large poster/card.

Overlay the important information on or around the card without destroying the poster presentation:

Entry title
Year
Media type / season when applicable
Overall rating in the upper-right area of the card
Existing “Top” badge when applicable

The rating should use the app's existing overall rating value/calculation. Do not create or modify the rating system.

The rating should be visually prominent but compact, similar to a small rating badge/circle in the upper-right area.

Stacking/layout behavior

On desktop, create a visually rich horizontal arrangement that takes advantage of the large available viewport.

Think of the layout as:

[large poster] [large poster] [large poster] [large poster] [large poster]

with the cards having a layered/stacked relationship, rather than:

[wide rectangular card] [wide rectangular card]

The cards should be large enough that the poster artwork is immediately recognizable.

Do not leave a huge unused empty area on the right side of the Overview.

If the number of entries exceeds the available width, use the appropriate carousel/overflow behavior rather than shrinking the posters into tiny cards.

Interaction

Clicking/tapping a card must open the details for that specific entry.

Reuse the application's existing entry-detail/view functionality rather than creating a separate duplicate detail system.

The interaction should feel natural for the stacked-card design.

If the design uses a focused/front card, make that card clearly interactive and ensure the selected entry's details are opened correctly.

Sections

Apply this same visual treatment consistently to:

Top Rewatch Picks
The country/category rewatch-pick sections such as Thai BLs That Hit Different
Chinese BLs Worth the Eternal Wait
Any other equivalent rewatch-pick sections generated by the existing Overview logic

Do not change which entries appear in these sections or their existing selection/ranking logic.

Responsive behavior

Desktop is the primary target because the current design wastes substantial horizontal space.

On desktop:

Make the poster cards large.
Use the available horizontal space.
Preserve the layered/stacked visual effect.

On smaller screens:

Adapt the number/size of visible cards appropriately.
Preserve the same stacked-poster visual concept rather than reverting to the old plain grid.
Important — do not repeat the previous design

Do NOT create:

Large horizontal rectangular cards
Two-column information panels
Poster on the left + title/details on the right
Large empty areas inside each card
Plain poster grids
A conventional dashboard/card layout

The attached reference should be treated as the visual direction: large portrait posters, layered/stacked cards, depth, strong visual presentation, and efficient use of horizontal space.

Preserve existing functionality

Do not change:

Rewatch-pick selection logic
Country/category grouping logic
Rating calculations
Stored entry data
Existing badges/data
Other Overview sections

Please inspect the current Overview implementation and existing reusable entry-detail components first, then replace only the presentation/layout of these rewatch-pick sections.

The most important requirement is that the result visually reads as a stacked poster-card carousel like the attached reference, not as a collection of wide rectangular cards.